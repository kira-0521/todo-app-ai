import type { Meta, StoryObj } from "@storybook/react";

import { DeleteButtonAction } from ".";

const meta: Meta<typeof DeleteButtonAction> = {
	component: DeleteButtonAction,
};
export default meta;
type Story = StoryObj<typeof DeleteButtonAction>;

export const Default: Story = {};
